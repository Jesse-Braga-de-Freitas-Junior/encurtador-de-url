// endereço do servidor HTTP url
export const config = {
	API_URL: 'http://localhost:5000',
	MONGO_CONNECTION:
		'mongodb+srv://<user>:<password>@<database>.yptlq.mongodb.net/url-shortener-dio?retryWrites=true&w=majority',
}
// a url do mongodb+srv: vai ser adquirida com a criação do banco, essa será a url de acesso ao banco.
